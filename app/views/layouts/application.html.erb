<!DOCTYPE html>
<html>
  <head>
    <title>Newquiz</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg bg-dark col-12">
        <div class="navbar-brand mx-auto col-8">
          So, I hear you guys like quizzes?
        </div>
        <div class="navbar-submit col-2">
              <!-- Button trigger modal -->
        <%= link_to 'Click Me!', '#', data: {toggle: 'modal', target: '#myModal'} %>
        </div>
      </nav>
    </header>

     <!-- Modal -->
    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content col-12">
          <div class="modal-header">
            <h4 class="modal-title" id="myModalLabel">Do you like quizzes?</h4>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">×</span>
            </button>
          </div>
          <%= simple_form_for Test.new do |f| %>
            <div class="modal-body">
              <%= f.input :answer, label: "Well, do ya?" %>
              <br />
              <%= f.input :name, label: "What's your name?" %>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <%= f.submit 'Create', class: 'btn btn-danger' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>



    <%= yield %>
  </body>
</html>
